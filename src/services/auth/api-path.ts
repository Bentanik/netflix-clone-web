const AUTH = "http://localhost:5001/api/v1";
const REGISTER_EMAIL = AUTH + "/register-email";
const LOGIN_EMAIL = AUTH + "/login-email";
const LOGOUT = AUTH + "/logout";
const REFRESH_TOKEN = AUTH + "/refresh-token";

export default {
  AUTH,
  REGISTER_EMAIL,
  LOGIN_EMAIL,
  LOGOUT,
  REFRESH_TOKEN,
};
